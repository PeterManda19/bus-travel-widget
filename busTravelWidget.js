// Application Logic
// Pseudo code in comments

// Define a function or factory function
function BusWidget() {
    // Add necessary properties and methods

    // Define methods for calculating various values
    // costPerTrip(points, destination)
    // totalTrips(points, destination, time)
    // returnTrips(points, destination, time, isReturn)
    // costPerReturnTrip(points, destination, time, isReturn)
    // priceBasedOnTime(destination, time)
}
  